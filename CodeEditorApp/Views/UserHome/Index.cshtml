@model List<CodeEditorApp.Models.ViewModels.ProjectViewModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@foreach (var item in Model)
{
    <h2>@item.name</h2>
    <br/>
    <h2>@item.location</h2>
    <br/>
}

